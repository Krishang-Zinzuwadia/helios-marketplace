# Helios Marketplace

A curated, local-first marketplace for discovering small language models that fit the Helios runtime.

## Development

```bash
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000).

## Status

This first release uses a deliberately hardcoded catalog while the model registry and installation flow are being designed.
